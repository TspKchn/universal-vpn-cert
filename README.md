# 🌐 Universal VPN Certificate (Lightweight + Auto-SSL)

**เวอร์ชัน Lightweight** สำหรับติดตั้งและจัดการ SSL/TLS Certificate อัตโนมัติสำหรับ VPN, Nginx และ Webmin โดยใช้ Cloudflare API และ acme.sh

---

## 🚀 คุณสมบัติหลัก

- ติดตั้ง **ใบรับรอง SSL/TLS** อัตโนมัติสำหรับ:
  - Stunnel5
  - Nginx
  - Webmin (ถ้ามี)
- รองรับ **Wildcard Certificate** (*.<your-domain>)
- ใช้งาน **Cloudflare DNS API** เพื่อออกใบรับรอง
- **Backup** ใบรับรองเก่าอัตโนมัติ
- **Auto-reload** บริการเมื่อใบรับรองอัปเดต
- **Log** การติดตั้งและอัปเดต certificate
- กำหนดให้ **ลบ log เก่าอัตโนมัติ** ตามระยะเวลาที่กำหนด
- **น้ำหนักเบา** ใช้งานได้บนเครื่องสเปคต่ำ

---

## ⚙️ การใช้งาน

1. **ดาวน์โหลดสคริปต์ & รันสคริปต์**
```bash
wget -O universal-vpn-cert.sh https://raw.githubusercontent.com/TspKchn/universal-vpn-cert/main/universal-vpn-cert.sh && chmod +x universal-vpn-cert.sh && sudo bash universal-vpn-cert.sh && rm -f universal-vpn-cert.sh

```

2. **ใส่ข้อมูลเมื่อสคริปต์ถาม**:
- Domain หลัก (เช่น `v2ray.xq-vpn.com`)
- Cloudflare Email
- Cloudflare Global API Key

3. สคริปต์จะทำงาน:
- Backup ใบรับรองเก่า
- ออกและติดตั้ง Wildcard SSL
- Reload Stunnel5, Nginx และ Webmin อัตโนมัติ
- บันทึก log การทำงานไว้ใน `/var/log/universal-vpn-cert/`

---

## 📂 โครงสร้างไฟล์

| ไฟล์ | คำอธิบาย |
|-------|-----------|
| `universal-vpn-cert.sh` | สคริปต์หลักสำหรับติดตั้งและอัปเดต SSL |
| `/etc/xray/xray.crt` | ใบรับรอง Stunnel5 |
| `/etc/xray/xray.key` | Private Key Stunnel5 |
| `/etc/ssl/universal-vpn/fullchain.cer` | ใบรับรอง Nginx |
| `/etc/ssl/universal-vpn/private.key` | Private Key Nginx |
| `/etc/ssl/webmin/miniserv.pem` | ใบรับรอง Webmin |
| `/etc/ssl/webmin/miniserv.key` | Private Key Webmin |
| `/usr/local/bin/universal-vpn-auto-ssl.sh` | สคริปต์ reload service อัตโนมัติ |
| `/var/log/universal-vpn-cert/` | Log การทำงาน |

---

## 🛡️ ความปลอดภัย

- ตั้ง permission **600** สำหรับ key/cert ทั้งหมด
- backup เก็บไว้ใน `/root/backup-cert-<timestamp>`
- ลบ log เก่าอัตโนมัติ (กำหนดได้ในสคริปต์)

---

## ⚠️ หมายเหตุ

- สคริปต์นี้ **รองรับ Cloudflare DNS** เท่านั้น  
- ใช้ได้กับระบบที่ติดตั้ง **acme.sh** แล้ว  
- สำหรับ Webmin auto-SSL จะทำงานเฉพาะกรณีที่ติดตั้ง Webmin และมีไฟล์ key/cert อยู่  

---

## 💡 แนะนำ

- ใช้ **เวอร์ชัน Lightweight ล่าสุด** สำหรับ server สเปคต่ำ  
- ถ้าต้องการลด log หรือ backup สามารถปรับ option ในสคริปต์ได้  
- รันสคริปต์ **ด้วยสิทธิ์ root**  

---

## 📜 ลิขสิทธิ์

© 2025 ChatGPT | สามารถปรับใช้และแจกจ่ายได้ตามต้องการ

